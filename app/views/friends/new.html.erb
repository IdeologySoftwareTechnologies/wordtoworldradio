<%= simple_form_for(@friend) do |f| %>
  <%= f.error_notification %>
<p id="notice"><%= notice %></p>
  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :friend_email %>
    
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Invite a Friend" %>
  </div>
<% end %>

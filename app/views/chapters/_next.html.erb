
<%# if @chapter.id == Category.last.bibles.last.chapters.last.id%>
  <%#= link_to category_bible_chapter_path(1,@bible.next,@chapter.next),class: "btn btn-default pull-right" do %> 
    <!--<i class="glyphicon glyphicon-chevron-right"></i>-->
  <%# end %>
<%#else%>
  <% if @chapter.id == @bible.chapters.last.id %> 
    <% if @bible.next.chapters.count == 0 %>            
      <% @first_bible = Bible.first%>
      <%= link_to category_bible_chapter_path(params[:category_id],@first_bible,@first_bible.chapters.first),class: "btn btn-default pull-right" do %> 
          <i class="glyphicon glyphicon-chevron-right"></i>
      <% end %>
    <%else%>            
      <%= link_to category_bible_chapter_path(params[:category_id],@bible.next,@chapter.next),class: "btn btn-default pull-right" do %> 
        <i class="glyphicon glyphicon-chevron-right"></i>
      <% end %>
    <%end%>
  <%else%>     
    <%= link_to category_bible_chapter_path(params[:category_id],@bible,@chapter.next),class: "btn btn-default pull-right" do %> 
      <i class="glyphicon glyphicon-chevron-right"></i>
    <% end %>
  <%end%>
<%#end%>


